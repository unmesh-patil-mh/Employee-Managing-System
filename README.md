# Employee Management System

## 📌 Overview
The Employee Management System is a Java-based application built using **JDBC** and **SQL** for managing employee records. It provides basic CRUD (Create, Read, Update, Delete) operations and helps organizations maintain employee data efficiently. The project was developed in **Eclipse IDE**.

---

## 🚀 Features
- Add new employees with details (ID, Name, Department, Salary, etc.)
- View employee records
- Update existing employee information
- Delete employee records
- SQL database integration using JDBC
- Console-based user interface

---

## 🛠️ Technologies Used
- **Java** (Core Java, JDBC)
- **SQL** (MySQL / any relational database)
- **Eclipse IDE**
- **JDK 8+**

---

## ⚙️ Setup Instructions
1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/employee-management-system.git
